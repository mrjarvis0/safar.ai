# Safar — environment template. Copy to `.env` and fill in. NEVER commit `.env`.
# The team lead provides the real keys. Add only the APIs you actually use.

# --- LLM (reasoning) ---
LLM_PROVIDER=mock            # mock | groq | gemini | openai | anthropic
LLM_API_KEY=

# --- Travel data APIs (fill the ones you have) ---
AMADEUS_CLIENT_ID=
AMADEUS_CLIENT_SECRET=
GOOGLE_MAPS_API_KEY=
WEATHER_API_KEY=
# add more as the project needs...
