"""Hotel agent — rank hotel options. Owner: adi.

Balance price / location / transit / stars / rating; explain with llm().
"""
from ..llm import llm
# from ..gateway.gateway import get_hotels


def run(state) -> list[dict]:
    # TODO(adi): options = get_hotels(state.trip)
    # TODO(adi): return scored options in the shared envelope
    return []
