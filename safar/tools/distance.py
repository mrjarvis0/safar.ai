"""Distance / ETA tool — deterministic (NO llm). Owner: adi."""


def km_between(a: tuple[float, float], b: tuple[float, float]) -> float:
    # TODO(adi): haversine, or Routes API via the gateway
    return 0.0
