"""Currency tool — FX conversion (NO llm). Owner: adi."""


def convert(amount: float, frm: str, to: str) -> float:
    # TODO(adi): static rate table, or an FX endpoint via the gateway
    return amount
